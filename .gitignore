# OS
.DS_Store
Thumbs.db

# Editör
.vscode/
.idea/
*.swp

# Bağımlılıklar (ileride Capacitor eklenirse)
node_modules/
dist/
# Capacitor webDir (build/www.js ile üretilir — kaynak değil)
www/
.gradle/
*.iml

# Capacitor / iOS / Android (ileride eklenirse)
ios/App/Pods/
ios/App/build/
android/.gradle/
android/app/build/
android/build/

# Refaktör/doğrulama artıkları (üretime dahil değil — build/ scriptleri tutulur)
build/shot-*.png
build/_*.html
build/_*.txt

# Eski monolit yedeği (yerelde durur, izlenmez)
yurtsim (2).html

# Ikon onizleme cikti klasoru (build/icon.js --preview)
build/_icon-preview/
